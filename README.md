This is a project in which I use following concepts export/import, setInterval to refresh time and price in every 1.5s.
I use Date() constructor to get the time and for price I use Math.random(). and for moving small triangle I use google fonts to import and some CSS property.
